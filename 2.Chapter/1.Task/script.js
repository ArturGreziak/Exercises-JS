// Temat ćwiczenia: “Metoda prompt() - pobieranie liczby od użytkownika”.Napisz skrypt, który będzie pobierał od użytkownika jedną liczbę przy pomocy metody prompt() oraz wartość ta zostanie przypisana do zmiennej typu consto nazwie liczba. Wypisz tą liczbę do konsoli tak jak pokazano w oczekiwanym wyniku.Oczekiwany wynik:Okno wywołane metodą prompt():Konsola:

const liczba = parseInt(prompt());

console.log(`Podana liczba: ${liczba}`);
