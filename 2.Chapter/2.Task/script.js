// Temat ćwiczenia: “Metoda prompt() - pobieranie łańcucha znaków od użytkownika”.Napisz skrypt, który będzie pobierał od użytkownika imię przy pomocy metody prompt() oraz łańcuch ten zostanie przypisany do zmiennej typu consto nazwie imie. Wypisz to imię do konsoli tak jak pokazano w oczekiwanym wyniku.Oczekiwany wynik:Okno wywołane metodą prompt():Konsola:

const imie = prompt(`Podaj imię: `);

// console.log(`Podane imie: ${imie}`);

alert(`Podane imie: ${imie}`)